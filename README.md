# Grid Trading Bot для Bybit (MVP v1.0)

[![Python 3.12](https://img.shields.io/badge/python-3.12-blue.svg)](https://www.python.org/downloads/)
[![Streamlit](https://img.shields.io/badge/Streamlit-1.41.1-red.svg)](https://streamlit.io/)
[![CCXT](https://img.shields.io/badge/CCXT-4.3.58-green.svg)](https://github.com/ccxt/ccxt)

Система для бэктестирования Grid Trading стратегии на исторических данных биржи Bybit. Проект предоставляет веб-интерфейс на Streamlit для настройки параметров сетки, загрузки данных, запуска симуляции и визуализации результатов.

## 📁 Структура проекта

```
.
├── config/
│   └── settings.py         # Конфигурация приложения (API ключи, параметры)
├── models/
│   ├── exchange_api.py     # Обёртка над ccxt для работы с Bybit API
│   └── grid_strategy.py    # Логика расчёта уровней сетки и проверки исполнения
├── services/
│   ├── data_loader.py      # Загрузчик исторических данных с кэшированием
│   └── backtester.py       # Движок бэктестирования (симуляция, метрики)
├── web_ui/
│   └── app.py              # Streamlit-интерфейс
├── .env.example            # Шаблон файла переменных окружения
├── requirements.txt        # Зависимости Python
├── ARCHITECTURE.md         # Детальное описание архитектуры
└── README.md               # Данный файл
```

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd bybit_grid_bot
```

### 2. Установка Python

Убедитесь, что у вас установлен Python 3.12 или новее. Проверьте версию:

```bash
python --version
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env`, указав свои API-ключи Bybit (опционально, для загрузки реальных данных):

```ini
# API ключи Bybit (можно оставить пустыми для тестовой сети)
BYBIT_API_KEY=your_api_key_here
BYBIT_API_SECRET=your_api_secret_here

# Параметры по умолчанию
SYMBOL=BTCUSDT
TIMEFRAME=15m
INITIAL_BALANCE=1000.0
GRID_LEVELS=20
GRID_STEP_PCT=0.5
ORDER_SIZE=0.001
```

**Примечание:** Для работы в тестовом режиме (testnet) ключи можно не указывать. Данные будут загружаться через публичный API биржи.

### 5. Запуск веб-интерфейса

```bash
streamlit run web_ui/app.py
```

После запуска откройте браузер по адресу [http://localhost:8501](http://localhost:8501).

## 🖥️ Использование веб-интерфейса

1. **Загрузка данных** – в боковой панели укажите торговую пару, таймфрейм и количество дней истории, затем нажмите «Загрузить исторические данные».
2. **Настройка сетки** – задайте верхнюю и нижнюю границы цены, количество уровней, размер ордера и начальный баланс.
3. **Запуск бэктеста** – нажмите «Запустить бэктест» для выполнения симуляции.
4. **Анализ результатов** – просмотрите метрики (PnL, Win Rate, Max Drawdown, Sharpe Ratio), график капитала и таблицу сделок.
5. **Экспорт** – при необходимости скачайте результаты в формате CSV.

## 📊 Пример работы

После запуска бэктеста вы увидите:

- **Ключевые метрики** – общая прибыль, процент прибыли, количество сделок, Win Rate, максимальная просадка, коэффициент Шарпа.
- **График капитала** – динамика equity (баланс + стоимость позиции) во времени.
- **Таблица сделок** – детализация всех исполненных ордеров (цена, сторона, объём, комиссия).
- **Дополнительная статистика** – распределение сделок по сторонам, объём сделок.

## 🔧 Разработка

### Запуск тестов

На данный момент модульные тесты не реализованы. Планируется добавить pytest-тесты для ключевых модулей.

### Расширение функциональности

- **Добавление новых стратегий** – унаследуйте класс `GridStrategy` и переопределите методы `calculate_levels` и `check_order_execution`.
- **Интеграция с другими биржами** – используйте класс `ExchangeAPI`, поддерживающий любую биржу из библиотеки CCXT.
- **Улучшение метрик** – модифицируйте метод `calculate_metrics` в `backtester.py`.

### Логирование

Логи пишутся в консоль с уровнем INFO. Для отладки можно изменить уровень на DEBUG в `web_ui/app.py`:

```python
logging.basicConfig(level=logging.DEBUG)
```

## 🛠️ Исправления после ревью

В ходе код-ревью были выявлены и исправлены критические и средние проблемы. Полный список исправлений с деталями реализации можно найти в [FIXES.md](FIXES.md).

**Основные исправления:**

- **Валидация API ключей только для mainnet** – теперь ключи проверяются только при работе в основной сети, в тестовом режиме (testnet) они не обязательны.
- **Задержка между запросами API** – добавлен `time.sleep(1)` для соблюдения rate limit биржи.
- **Чередование сторон ордеров в сетке** – уровни теперь чередуются (чётные – покупка, нечётные – продажа) вместо простого разделения по половинам.
- **Правильный расчёт Win Rate** – реализован на основе сопоставления покупок и продаж по методу FIFO.
- **Исправление расчёта Sharpe Ratio** – используется annualization factor `sqrt(252)` для торговых дней.
- **Кэширование данных в Streamlit** – загруженные данные сохраняются в `st.session_state`, что исключает повторную загрузку при каждом нажатии кнопки.
- **Всегда отображаемая статистика данных** – раздел «Статистика данных» теперь всегда виден, даже если данные ещё не загружены.

Все исправления прошли проверку синтаксиса и готовы к тестированию. Подробности см. в [FIXES.md](FIXES.md).

## 📈 Архитектура

Подробное описание архитектуры, потоков данных и компонентов системы см. в [ARCHITECTURE.md](ARCHITECTURE.md).

## 🤝 Вклад

1. Форкните репозиторий.
2. Создайте ветку для новой функциональности (`git checkout -b feature/amazing-feature`).
3. Внесите изменения и закоммитьте (`git commit -m 'Add amazing feature'`).
4. Запушьте в форк (`git push origin feature/amazing-feature`).
5. Откройте Pull Request.

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. Подробности см. в файле LICENSE (если есть).

## ⚠️ Отказ от ответственности

Данный проект предназначен исключительно для образовательных и исследовательских целей. Авторы не несут ответственности за любые финансовые потери, возникшие в результате использования этого кода. Торговля криптовалютами сопряжена с высокими рисками.

---

**Авторы:** Команда разработчиков Grid Trading Bot  
**Версия:** MVP v1.0  
**Дата:** 2025# crypto-trading-bot
